[
 "[p5-d1-batch1-items.json] g1 and g4: facet field is the assigned, previously-unused facet id (F-1.1-05, F-1.7-02) per the dispatch slots, but invGuidance for both directs the actual inversion content at a conceptually-related, already-shipped row in the same section (the override-rate/proxy-signal row of 1.1 for g1; the content+metadata citation row of 1.7 for g4) rather than the assigned facet id's own row. This mirrors Paper 4's shipped g1 pattern (facet F-1.1-04 with override-rate content) and is treated as intentional, not a mismatch to flag as an error.",
 "[p5-d1-batch1-items.json] g6: per dispatch instructions, this item was built by combining one independently-correct sub-action each from two different decision-table rows in 1.12 (moving enrichment off the request path from the p95-latency row, and confidence-tier routing from the cost-pillar row) rather than using the assigned facet F-1.12-05's own row (non-engineer stakeholder communication) or splitting either row's own compound answer into two options. Flagging this construction choice for audit transparency.",
 "[p5-d1-batch1-items.json] g6 factAnswerRaw is a '||'-joined pair of verbatim corpus fragments, one from each of the two source rows used (see prior note), since the two correct options draw from two different table rows rather than one facet's single Answer-column text. Flagging so central lessonKey assembly can split or handle the compound string deliberately rather than treating it as one literal answer string.",
 "[p5-d1-batch2-items.json] g9: slot data lists kind=\"misconception\"/id=\"M-1.4\" but direction=\"inverted\" with facet-style invGuidance (an under-engineering rung inversion), which appears to conflict with the shared brief's general rule that misconception-unit items always ship direction=\"normal\". Authored exactly per the dispatch prompt's explicit instruction and the invGuidance text (a decision-table-style inversion of section 1.4's ladder logic — a hybrid fixed-pipeline-plus-one-agentic-substep design), not from the corpus's literal Misconception prose block. factAnswerRaw is populated as the new correct answer's own text per the inverted-item rule. Flagging for the assembly/audit pass to confirm this override was intentional at central planning.",
 "[p5-d2-batch1-items.json] g15 (F-2.6-02): mapped to the third row of 2.6's new 'Scope of the Fix' table (scattered facts, no single dominant fact -> headed sections grouped by topic) based on content fit against the invGuidance-free slot; FACET-LEDGER.md was not part of this batch's required reading, so exact facet-to-row numbering wasn't independently confirmed against it.",
 "[p5-d2-batch1-items.json] g13 (F-2.2-05, inverted): T1's alt (option A, blanket top-level exception) is grounded in the logical converse of the Guardrail Scope table's stated row (per-layer scoping is needed only when behavior must differ by tool/sub-agent; if the need became uniform across all layers, a blanket rule follows) rather than a row stated in that exact form in the corpus -- flagging for the grounding audit to confirm this converse reading is acceptable rather than treating it as a fabricated row.",
 "[p5-d2-batch1-items.json] g13 (F-2.2-05, inverted): section 2.2's system-prompt-authority rule is close to absolute, as the brief itself flags for this section/shape pairing; the S8 framing (scoped exception vs. removal) was used per invGuidance rather than the section's usual S2 hint, and no IRREDUCIBLE flag was needed since a clean T1 resolution was found (see t1Clause), but this item is a good candidate for extra audit scrutiny given the section's known difficulty.",
 "[p5-d2-batch2-items.json] g16 (inverted, F-2.8-02): the correct answer's mechanism (dynamic content placed after the stable prefix) surface-resembles 2.8's static-first ordering rule, but the tested lesson is distinct — resolving an apparent conflict between a literal 'disclaimer must be first' compliance reading and cache preservation by correctly scoping what the requirement actually demands (presence/prominence, not byte-0 position), per invGuidance. Flagging for the audit's rule-1 (genuine-difference) check; not raised as an IRREDUCIBLE case.",
 "[p5-d3-batch1-items.json] g23 (F-3.6-04): T1 resolution is softer than the batch's other items. Section 3.6's decision table keys this row on a single trigger condition (traces will contain regulated content) rather than a contrasting pair; t1Clause inverts that trigger (no regulated content reaches the trace) and t1Alt=A assumes standard security hygiene becomes adequate once the row-4 mandate no longer applies, which is a reasonable inference rather than a row stated outright. Not IRREDUCIBLE -- the item's own correctness and its T2/T3/T4 pass cleanly -- but worth the grounding audit's confirmation on T1 specifically.",
 "[p5-d3-batch2-items.json] g28: slot was assigned kind:misconception/id:M-3.3, since section 3.3's four decision-table facets (F-3.3-01..04) are already shipped across Papers 1-4 (confirmed against FACET-LEDGER.md and MockTest-1/4 output). Per the dispatch instruction to build a genuine 2-of-4 multi-response item around two independently-correct, jointly-required sub-actions rather than split a single fact, the item is grounded in M-3.3's own correction text (a service account requires each user's entitlement to be re-established at the query -- i.e. identity propagation AND query-time filtering), which happens to state the same underlying fact as F-3.3-01's answer. Kept facet:'M-3.3' per assignment and direction:normal per the misconception rule. Flagging for the dedup audit to confirm no shape collision with Paper 1's g28 (F-3.3-01, single-format) or Paper 4's g29 (F-3.3-04, multi-format, correct pair = identity propagation + reject-system-prompt-rule) -- this item's correct pair (identity propagation + query-time filter enforcement) and distractor set (system-prompt rule, per-user credential substitution) differ from both, but all three now share the same section and underlying mechanism.",
 "[p5-d3-batch2-items.json] g29: family tag DETECTIVE-FOR-PREVENTIVE applied to option D (a post-hoc SLA-breach alert vs. fixing the pipeline's slowest stage) extends the canonical security/misuse-monitoring definition into a latency-observability context. Flagging as a judgment call for the family-balance audit in case a different label fits the paper-wide tally better.",
 "[p5-d3-batch2-items.json] g31 (inverted): t1Clause/t1Alt is a softer resolution than the paper's other items -- it turns on the proposal's motivation (pure cost vs. a genuine correctness signal such as retrieval ambiguity) rather than a crisp stem clause with a clean binary flip. This is a structural property of 'reject a change and keep the status quo' inverted items rather than a fresh error; flagging for the grounding audit's awareness, not asserting a full IRREDUCIBLE.",
 "[p5-d4-batch1-items.json] g34: slots file tags this item kind='misconception' / facet='M-4.9' with direction='inverted', which on its face conflicts with the shared brief's rule that M-<section> misconception-unit items always ship direction:normal. Authored per the dispatch's explicit invGuidance and shape S3 (post-change diagnosis, inverted — 'the recent change is a coincidence; the pinned variables point elsewhere'), treating M-4.9 here as a fresh scenario anchor rather than a literal build from 4.9's boxed Misconception paragraph. Flagging for the assembly/audit to confirm this override was intended, not an authoring error.",
 "[p5-d4-batch1-items.json] g33: t1Alt=C treats 'Recall@5 already tracked separately at 0.55/0.97' as the inversion of 'single blended score, regression cannot be located,' mapping to section 4.7's own row ('Retrieval Recall@5 = 0.55, generation faithfulness = 0.97 -> Both bars are read; the system fails on retrieval'). Not IRREDUCIBLE — documenting the mapping for the grounding audit.",
 "[p5-d4-batch1-items.json] g36: multi-select draws two independently-correct sub-actions from two different rows in 4.11's 'identical preamble' scenario cluster (F-4.11-01's caching answer and F-4.11-02's reject-the-cut answer), per the dispatch's instruction to build a genuine 2-of-4 item rather than split one fact. The assigned facet F-4.11-03's own row (reject the extra-model-call summarization) appears as this item's option C distractor (ARCHITECTED) to keep the item inside the same scenario cluster as its assigned facet without overlapping whatever row the sibling single-answer item (g41) covers. factAnswerRaw is accordingly a verbatim join of two Answer-column strings rather than one. t1Alt=A is inferred from row F-4.11-02's stated 'why' (a compliance regression, not a saving) reversed, rather than a separate literal row — not IRREDUCIBLE, flagging the inference for audit confirmation.",
 "[p5-d4-batch2-items.json] g41 (F-4.11-07, S1 normal): the ARCHETYPE-LEDGER's normal-direction gloss for S1 is 'the principle demands removing something; the distractors compensate instead,' which doesn't literally fit a measurement-first answer ('profile first' removes nothing). Kept S1 rather than substituting to S6 because the underlying mechanics still fit S1's stated column definition ('states a governing principle by name... apply it against options defensible on some other axis') — the governing principle is 'profile before optimizing,' and all three distractors are real 4.11 levers (caching, cascade routing, retrieval trimming) that are correct elsewhere but premature here. Flagging for the grounding audit to confirm this reading is acceptable rather than a forced fit.",
 "[p5-d5-batch1-items.json] g43 (multi, section 5.6): built from two independently-correct rows of the Diagnosis Discipline table -- F-5.6-07 (reindex-day confident wrong answers -> investigate retrieval pipeline: chunking, embedding version, index freshness) and the same-day vendor version-update row (same prompt/code, different behaviour -> check pinned model version and regression set). No single verbatim Answer-column string covers both, so factAnswerRaw is left as \"\" per the brief's explicit rule for this case, consistent with the Paper 3 g43 precedent.",
 "[p5-d5-batch1-items.json] g44 (reuse-inverted, anchor F-5.8-01, exhausted facet): self-checked against the cosmetic-restate failure Paper 4 shipped once (F-27/F-28 standing caution). The anchor's normal lesson is 'route by confidence AND consequence together, not blanket review or naive sampling,' under volume/cost pressure. This item instead teaches 'a hard SLA and blanket human review cannot both hold -- name the binding constraint and route by threshold,' under a time/SLA pressure. The correct answer (auto-approve within the window, route above threshold even past it) and the primary trap (option A, add reviewers to force full review inside the SLA) are new content, not a reworded version of the anchor's own blanket-review distractor. Judged a genuine inversion, not IRREDUCIBLE.",
 "[p5-d5-batch1-items.json] g45 t1Alt=C resolves to F-5.3-04's own decision-table row (the same facet this item is built from) rather than a different facet, because the constraint-sensitivity clause here is exactly the absolute-vs-preference line that separates F-5.3-04 from the section's other 5.3 rows -- this is the intended mechanism per invGuidance (the item's correct answer is the opposite of what F-5.3-04 normally teaches), but flagged for a second look at grounding audit since the alternate resolution circles back to the anchor facet.",
 "[p5-d5-batch2-items.json] g48: t1Alt=C maps to 5.9's free-text-summary-quality row (second-model/rubric check), which is directionally sound, but that row's full answer also requires stating the check's correlation limits -- option C's wording ('post only when both agree') doesn't include that hedge. Flagging for the grounding audit to confirm this T1 resolution rather than asserting it as a clean 1:1 row match.",
 "[p5-d6-batch1-items.json] g52 (D6 6.4): dispatch assigned facet id M-6.4 (the section's misconception unit) as the anchor for a direction-inverted S7 item, per task-level instruction that pairs it with g51 as the same S7 inversion pattern on a different section. This overrides the shared brief's general 'misconception units never invert' rule for this one slot. The item was authored as a fresh decision-style S7 item, not the misconception normal-direction schema; factAnswerRaw carries the new correct answer's own text, matching how the brief's reuse-inversion mechanism treats anchor facets for O3.1/O5.3 elsewhere.",
 "[p5-d6-batch1-items.json] g53 (D6 6.10): invGuidance required a fresh 'measurement exists but is misread due to a confound' scenario (satisfaction-metric uptick confounded by a seasonal case-mix shift). This is not a literal row in 6.10's four-row decision table; it is grounded in the section's stated discriminator and drift-direction language (a recurring, controlled measurement beats a single uncontrolled reading) but t1Alt (option A, crediting the process change once the seasonal confound is removed) is a reasoned inversion of the tested principle rather than a named table row. Flagging as a likely T1 case for the grounding audit to confirm.",
 "[p5-d7-items.json] g60/F-7.2-04: the anchor facet is itself a 'Reject' row ('adding be concise to the skill') with no positive Answer-column text of its own. Section 7.2 supplies only two positive mechanism rows (F-7.2-01 allowed-tools, F-7.2-03 context:fork) that pair into a genuine 2-of-4 multi-response item, and both Paper 1's and Paper 4's own 7.2 multi-response items already landed on that identical pairing under different surface nouns (dependency-update-PR/migration; code-review/git-rebase). g60 lands on the same pairing again for the same structural reason and cannot avoid it given the corpus's current supply. The scenario here (a supply-chain team's price-feed-scanning skill with a live warehouse-stock-adjustment command) is a different industry and concrete task from both priors, but flagging explicitly per dispatch instructions so the independent audit checks g60 against all four prior papers' D7 items specifically, not just a stem-similarity score.",
 "[p5-d7-items.json] g63's t1Alt (A) is a soft resolution, flagged the way Paper 4 flagged its own weakest D7 T1: section 7.1's decision table doesn't tabulate a 'coincidental config edit vs. simultaneous CLI-version default change' diagnostic branch. That root-cause-diagnosis logic sits closer to section 7.7's diagnostic-rung ordering (rung 2, is it the right configuration layer; the changelog check is evidence-gathering before attributing cause) than to 7.1's own scope/durability table. Recording as a likely near-IRREDUCIBLE case for the grounding audit to confirm rather than asserting a clean row match.",
 "[p5-d7-items.json] This batch's inline-code-token usage (allowed-tools, context: fork, settings.json, deny, PostToolUse, --output-format json, --json-schema, -p/--print, CLAUDE.md, .claude/rules/) runs well above the paper-wide 15% target within just these 4 items. D7 is not on the banned list (D1/D5/D6) and sections 7.2/7.5/7.8's own decision tables are themselves dense with these literal config tokens, so left in place for central paper-wide rebalancing rather than stripped for an artificial local target — consistent with Paper 4's own D7 batch note on this same point."
]