1. [D1] The coordinator's agentic loop reads each response's `stop_reason`, and when it equals `tool_use`, extracts and executes exactly one `tool_use` block before calling Claude again. In a research run needing three independent searches, logs show only the first `Task` call each round is ever executed, even though the response actually carried three `tool_use` blocks side by side. Investigations that should run in parallel instead complete one round at a time. What should the loop implementation do?
2. [D1] A recent refactor consolidated the coordinator's tool configuration into a shared file alongside three subagent definitions. Since the merge, the coordinator investigates every request in a single long turn instead of delegating — its system prompt still names the web-search, document-analysis, and synthesis subagents individually, and none of the subagents' own tool restrictions changed. What should the team check first?
3. [D2] A teammate proposes two ways to give the web-search subagent visibility into which utility territories and study types currently have filings on record: expose it as an MCP resource, or add a `list_categories` tool the subagent can call each time. Call-count savings aside, what is the strongest reason to prefer the resource?
4. [D1] Asked to assess grid readiness for a new battery-storage interconnection request, the coordinator decomposes the work into three subtasks: transmission-capacity studies, interconnection-queue position, and equipment lead times. All three subagents complete successfully and the report reads as thorough, but it never addresses land-use permitting, which the requesting utility flags as the actual blocker. What is the most likely root cause?
5. [D5] Synthesis receives a combined 90,000-token input assembled from all four subagents in a fixed order: search results first, document-analysis findings second, then two rounds of `verify_fact` confirmations. Reports consistently open with search headlines and close with the confirmed facts, but a transmission-capacity finding sitting in the document-analysis section, roughly a third of the way through, is missed in nine of the last ten runs even though it is the report's most decisive finding. What should the coordinator change?
6. [D1][select-2] The synthesis subagent's prompt currently receives only the research question and its own scoped `verify_fact` tool — none of the web-search or document-analysis subagents' findings are included. Its draft reports read as generic and undercite specific figures found earlier in the pipeline. Select two changes that correctly address this.
7. [D2] Two tool-pairing patterns show up in the coordinator's logs. The document-analysis subagent calls `analyze_study` and then `extract_citations` on the same document in nearly every run, with no dependency between the two calls' inputs. The report-generation subagent's `render_report` call always follows `preview_report`, and only `preview_report`'s output supplies the token `render_report` requires. Which pairing is the better candidate for prompt-level bundling into a single turn?
8. [D5] By the fourth interconnection study in a run, synthesis has started attributing a contractor's internal revision notes to the study's actual capacity findings, blending the two together in its draft. Tracing the cause, `analyze_study` returns each study's full metadata block — revision history, contractor comments, filing timestamps — alongside the handful of figures synthesis actually needs, and none of it is filtered before entering context. How should the team address this?
9. [D1] When `search_registry` cannot reach the interconnection-queue database, the web-search subagent currently returns the string `search failed` to the coordinator, with no other detail. The coordinator cannot tell whether to retry with adjusted parameters, continue with partial results from other sources, or escalate, and in practice it always chooses to retry the exact same call. What should the subagent return instead?
10. [D2] The `verify_fact` tool's input schema declares a `scope` parameter as a free-text string, with three example values shown in its description block: `single-study`, `queue-wide`, and `utility-territory`. In practice the synthesis subagent sends values like `this study` and `the whole filing`, which match none of the three, and downstream handling silently treats them as `single-study`. What is the most effective fix?
11. [D3] The research team wants three things available to Claude Code sessions: a citation-format rule that must apply in every session regardless of task, a stricter sourcing-verification convention that should apply only when a session is editing files under the `synthesis/` directory, and a `/rebuild-report` workflow that assembles a final report on demand. Which configuration correctly matches each to its mechanism?
12. [D2][select-2] The document-analysis subagent's `analyze_study` and `extract_citations` calls have been bundled into one turn via a prompt instruction for months, and round-trip counts have stayed low. A teammate proposes replacing the pair with a single composite `analyze_study_with_citations` tool, since the two are always called together now. Select two statements that correctly evaluate this proposal.
13. [D1] After a first refinement round, the coordinator re-delegated a targeted query on interconnection-queue position and re-invoked synthesis; that section is now solid. A teammate suggests a third round anyway, with no specific gap identified. Every quality criterion the coordinator was given is already met. How should the coordinator proceed?
14. [D5] The document-analysis subagent finds a 2023 study estimating a 28-month average queue wait; the web-search subagent separately surfaces a 2026 industry post citing 42 months for the same queue. Synthesis flags this as a direct contradiction and asks the coordinator to reconcile which number is right. Neither subagent's output records when its source was published or collected. What is missing from the pipeline?
15. [D1] Before publishing, the coordinator has the synthesis subagent that wrote the draft re-read its own output to confirm the capacity conclusions hold up. It reports high confidence. A human planner reviewing it a week later finds a contradicting capacity figure within minutes. What most directly explains why the self-check missed it?
16. [D1] The developer-tooling assistant's custom harness decides whether to keep its tool-calling loop running by scanning each response's text for phrases like 'Done' or 'No further changes needed,' rather than inspecting the API response itself. During a `fulfillment` module investigation, the loop stopped early when Claude's intermediate text read 'Done searching the `auth/` subdirectory, moving to `billing/` next.' What should the harness check instead to decide whether to continue the loop?
17. [D2] The team wants a new lint rule applied to every TypeScript test file in the `fulfillment` module, but the files sit beside their source code, inconsistently named — `*.test.ts` in some folders, `*.spec.ts` in others. Which built-in tool should the assistant use to enumerate every matching file by name?
18. [D2] A `repo-navigator` subagent carries sixteen tools — six built-ins plus ten MCP tools for issue tracking and CI status. It frequently calls the wrong one, once invoking a CI-status tool to find a function definition. A peer subagent scoped to four role-relevant tools selects correctly almost every time. What is the most effective fix?
19. [D2] While updating the `fulfillment` module's retry handling, the assistant needs to change the timeout inside one specific `catch` block in `services/fulfillment/retryHandler.js`. It calls `Edit` with the anchor `retries += 1;`, but that line recurs in four other catch blocks with identical surrounding code, so `Edit` cannot tell which one to modify. What is the correct fallback for a reliable, targeted change?
20. [D1] Eleven days ago, in a named session, the assistant built a dependency map of the `notifications` module — event triggers, downstream consumers, retry queues. Since then, a teammate renamed two consumer classes and split a third into two files during an unrelated cleanup. The engineer wants to continue today without redoing the whole map or reasoning over now-stale results for those consumers. What is the best way to continue?
21. [D2][select-2] The assistant exposes separate `read_file_history` and `read_open_issues_for_file` tools. Telemetry shows engineers request file history alone in about 80% of investigations, and request both together only when they explicitly ask for 'full context' on a file, roughly 20% of the time. A teammate proposes a composite `get_file_context` tool that always returns both, citing the general principle that composite tools reduce round-trips. Select two considerations that argue against building the composite here.
22. [D1] An engineer wants the assistant to add integration tests across the legacy `notifications` module. Full scope is unknown up front: two submodules have zero tests, one has partial coverage, and mid-work the assistant discovers a submodule shares one mutable connection pool with an unrelated billing job, which changes the safe test order. A teammate proposes enumerating every file first and writing tests in a fixed, predetermined order. Which decomposition strategy actually fits this task?
23. [D3] The team's `/perf-profile` skill runs a synthetic load test against the `fulfillment` module and prints latency percentiles for every endpoint — often several hundred lines. After an engineer runs it mid-task to spot-check one slow endpoint, the assistant's later replies grow noticeably slower and drift from the refactor the engineer was actually doing. How should the skill be reconfigured to fix this without losing the full profiling output?
24. [D1] After the assistant finishes mapping how the `reporting` module reads data from six upstream services, the team wants to compare two fixes for its latency problem — precomputing daily aggregates on a schedule, versus adding a caching layer in front of the existing live queries — both starting from that same completed mapping. Which approach lets the team compare both without either evaluation's reasoning contaminating the other?
25. [D2] An MCP server exposes `find_similar_logic`, an embeddings-based search that finds functionally similar code even when the wording differs, meant to help the assistant spot duplicate implementations. Its description is one line: `input: query string`. Genuine near-duplicate functions are scattered across `services/`, yet the assistant keeps reaching for built-in `Grep` on exact-string queries and never calls `find_similar_logic` unprompted. What is the most effective way to fix the fallback?
26. [D1] While generating discount-calculation helpers for the `billing` module, the assistant's reasoning trace considers a rounding edge case for negative refunds, judges its approach sufficient, and moves on. A human reviewer later catches an off-by-one-cent bug in exactly that edge case during PR review. The team wants to catch this class of self-dismissed issue before human review. Which approach most directly addresses the root cause of this self-check limitation?
27. [D1][select-2] A `codebase-mapper` subagent scans the `fulfillment` module for every external API call site. It completes 32 of the module's 40 files, then times out on 8 remaining, unusually large generated files. It returns only `{"status": "failed"}`, leaving the coordinator unable to retry, skip, or proceed. Select two changes that would most effectively fix this.
28. [D3] Two items sit on the team's backlog: consolidating the monolithic `fulfillment` service into three independently deployable services, and fixing a null-pointer dereference in `calculateSurcharge` at the exact line a stack trace identifies. Which of the following most clearly calls for direct execution rather than planning mode?
29. [D1] Retry logic across the `fulfillment` module is the next mapping target. The assistant's investigation plan searches only `services/fulfillment/retry/`, and its report on that folder is accurate. It misses two other places retry logic lives: a shared `lib/http-client` wrapper and a scheduler cron module. What is the most likely root cause?
30. [D1] Three independent legacy submodules — `billing`, `notifications`, `reporting` — need investigation before a shared refactor plan. The assistant investigates them in three serial turns, starting each `Task` call only after the last one returns — a full cycle takes nine minutes. How should the three investigations be restructured to cut this latency?
31. [D4] The pipeline's payout-eligibility calculator needs an `incident_classification` tool call to run before a separate `repair_cost_enrichment` tool augments the record with regional labor-rate benchmarks. Logs show enrichment sometimes fires on claims the classification tool never touched, using stale default benchmarks and skewing payout estimates. Which `tool_choice` setting on the first call fixes this?
32. [D4] Repair-cost figures reach the extraction schema in mixed shapes: `$1,240.00`, `1240`, and `840-960` where an adjuster wrote a range instead of a figure. The payout calculator, which expects a single decimal number, has started throwing type errors on roughly 6% of claims. What should the team change?
33. [D5] A multi-year claim history compiled for a fraud-review pass runs to roughly 60K tokens: adjuster notes, prior repair records, and correspondence. The review step reliably references the earliest notes and the most recent correspondence but misses a policy-lapse period documented only in the middle third of the file. How should the compiled history be restructured?
34. [D4] The extraction tool's JSON schema is maintained by hand in the tool definition, while a separate Pydantic validator enforces the same fields after extraction. The two have drifted twice this quarter — most recently, the validator required `policy_number` while the tool schema still marked it optional. What is the correct fix?
35. [D5] Two body-shop estimates for the same damaged panel arrive attached to one claim: one lists $1,850 for a full panel replacement, the other $960 for a repair. Both shops are on the approved-vendor list and both estimates look complete. What belongs in the structured record here?
36. [D5][select-2] Field-level confidence scores accompany every extracted claim record, and the team wants to decide what happens next. Auto-processing already applies above a fixed confidence cutoff, and reviewer time is limited. Select TWO changes that correctly implement calibrated, capacity-aware human-review routing.
37. [D1] A claim packet often includes an incident report, a repair estimate, and adjuster notes as three separate documents. The pipeline currently extracts them one call at a time — incident report first, then a second call once that completes, then a third — and average packet-handling time is 40 seconds even though no document depends on another's output. How should the three calls be restructured?
38. [D4] The `incident_type` field is declared as a free-text string. A downstream business rule rejects values it doesn't recognize, and near-miss variants — `rear end collision`, `rear-end collision`, `collison` — fail validation at a steady 9% rate. Each failure retries with the validation error appended, but the retry rate hasn't moved in three weeks of tuning the feedback wording. Which change addresses the failure instead of further retry-loop tuning?
39. [D5] An overnight run processes 8,000 claim documents through the extraction pipeline. It crashes after roughly 5,300 with no persisted record of which had completed, so the operator re-submits the full 8,000 the next morning — including the 5,300 that already succeeded. What should the pipeline do instead?
40. [D4] A repair estimate states a total of $2,150. The extracted line items for it sum to $1,890. The extraction schema currently records only the stated total, and the $260 gap surfaced five weeks later, at reconciliation. What schema change would catch this at extraction time?
41. [D4] The schema marks `witness_name` and `third_party_insurer` as required strings. Roughly a third of claims are single-vehicle incidents with no witness and no third party involved. Spot checks on those claims find plausible-looking names and insurer names that don't appear anywhere in the source documents. Which change removes the pressure to fabricate these two fields?
42. [D4][select-2] The team is moving overnight extraction volume onto the Message Batches API. One engineer claims tools cannot be used inside a batch request at all; another proposes matching each result back to its claim by its position in the returned array. Select TWO statements that correctly describe the Batch API.
43. [D5] A claims-summary report synthesizes findings from the incident report, the repair estimate, and adjuster notes into a single narrative paragraph per claim for the adjuster dashboard. Adjusters now ask which document supports each stated fact, and the synthesis step has no way to answer — the individual extraction outputs were merged into prose before reaching it. How should extraction and synthesis preserve that link?
44. [D4] Extracted claim records must reach the payout calculator within 32 hours of a claim packet arriving. The team plans to submit each day's packets as a single Batch API run and needs to set a submission deadline relative to packet arrival. Given the batch window's worst case, what is the latest that submission can happen?
45. [D5] Before extraction can run, incoming claim packets must be triaged into auto, property, or liability categories by scanning document text across a 3,000-packet backlog. Running this triage inline floods the main pipeline session with scan output before extraction even starts. Where should the triage scan run instead?
46. [D3] Finding counts across the last four sprints feed into a weekly summary, built by hand: a team member re-reads each review's narrative output and copies severity and file counts into a spreadsheet. Two recent summaries used inconsistent severity buckets. What should the review step produce instead?
47. [D3] The review step must always flag leftover TODO comments in changed code — a rule the whole team wants enforced in CI. The instruction currently lives only in the pipeline engineer's personal `~/.claude/CLAUDE.md`. CI runs cleanly, since that engineer's own machine built the pipeline config, but a colleague's local dry-run of the same reviewer never flags TODOs. What is the correct fix?
48. [D4] A drafting step turns each finding into PR comment text. Findings that are self-contained — most naming and style issues — can be worded directly from the finding data. Findings that reference a shared convention not restated in the finding itself need the step to call `lookup_related_file` and quote the relevant passage first. The split runs close to 60/40. Which `tool_choice` setting fits the drafting step?
49. [D3] A `/audit-deps` skill scans the dependency tree for known-vulnerable package versions before the review step runs, printing several hundred lines of transitive-dependency detail into the conversation. Afterward, the review step's comments start reasoning about unrelated dependency chains instead of the diff in front of it. Which SKILL.md frontmatter change fixes this?
50. [D3] Each time an author pushes a follow-up commit, the review step re-runs from scratch and re-posts several comments that are nearly identical to ones already sitting, unresolved, on the same lines from the previous run. Developers have started ignoring the newest comments on the assumption they are repeats. What should the re-run prompt include?
51. [D2][select-2] Before approving a PR that removes a helper function, the review step must confirm every call site was updated. The function, `formatDate`, is wrapped by a utility module as `getFormattedDate` and re-exported from the package's index file. Searching only for the literal string `formatDate` finds the definition and a few direct callers but misses every caller that imports the wrapper. Select TWO statements that are correct.
52. [D3] The review step must always close its comment with a one-line overall risk rating, on every pull request regardless of which files it touches. A developer notices the closing line is missing from about a third of recent reviews and proposes adding a new `.claude/rules/review-format.md` file, glob-scoped to the changed files, so the rule 'reliably loads.' What is the correct response?
53. [D4] Roughly a third of PRs come back with no findings at all. A closing step recommends what happens next for each one: a plain-text merge recommendation when clean, or a call to its only defined tool, `estimate_fix_effort`, to attach a time estimate when findings exist. The team sets `tool_choice` to `{"type": "any"}` so 'the output is always structured.' What is wrong with that choice?
54. [D3] An investigation from three weeks ago traced a specific false-positive pattern across five review-step modules. Since then, an unrelated refactor rewrote all five modules; only the finding's general shape still applies. The plan is to `--resume` it today and continue where it stopped. What is the better approach?
55. [D3] To cut cost, a weekly dependency-license audit is moving to the Message Batches API. Its prompt defines a `flag_license` tool so findings arrive as structured data, and each request is a single self-contained document with no follow-up needed. An engineer objects that batch requests 'can't define tools at all.' Is that objection correct?
56. [D4] Generated code merges after passing its own 'double-check your work before finishing' step, but production bugs traced back to that code keep recurring. The generating session's transcript shows the double-check step re-reading its own reasoning and confirming the original approach each time. What change would most directly fix this?
57. [D2][select-2] Coverage on a PR falls below the required threshold — a policy limit, not a malfunction — and the `merge_gate` tool that should block auto-merge for that reason instead returns a bare `{"isError": true}` with no other detail. The calling step retries the call three times, then escalates the PR to a human as though the tool itself were broken. Select TWO changes that fix this.
58. [D3] Three unrelated problems turn up in a generated test file: an assertion checks the wrong error message, a test for empty input is missing, and a fixture file carries a mismatched date format. None of the three affects how the others should be fixed. How should this feedback be delivered to converge fastest?
59. [D3] Generated boundary tests for a discount calculation pass locally but miss one case: a discount that brings the price to exactly zero. Three rounds of asking Claude to 'handle edge cases more thoroughly' have produced three different, still-incomplete fixes. What is the fastest way to converge on the missing case?
60. [D4] Most changed functions get new test code directly from a test-generation step, with no tool call involved. For functions tagged `safety-critical` — roughly one in five — the step must also call `register_coverage_entry` to log the new test in a compliance tracker. The team configures the step with `{"type": "tool", "name": "register_coverage_entry"}` on every run so tracking is 'never missed.' What is wrong with this setting?