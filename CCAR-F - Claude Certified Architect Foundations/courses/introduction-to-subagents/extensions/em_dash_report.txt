L6 (x1): <title>Subagents as a Programmable Primitive (CLI → Agent SDK) — Extension</title>
L235 (x2): <div><b>Beyond the Academy course.</b> This is web-researched material grounded in cited Anthropic primary sources — the Claude Agent SDK subagents / TypeScript / Python references, the Claude Code "create custom subagents" and "workflows" docs, and the "Build...
L243 (x2): <p style="margin:0 0 6px"><span class="emoji">🔗</span> <strong>Where this sits in the ladder.</strong> It builds on the <a href="../introduction-to-subagents.html">← Introduction to Subagents (base course)</a> — the "what is a subagent / when to delegate" foun...
L257 (x1): <li><strong>Declare</strong> a subagent as an <code class="inline">AgentDefinition</code> in TypeScript and Python — and avoid the camelCase-in-Python <code class="inline">TypeError</code> trap.</li>
L266 (x2): <b>🎯 Why this matters for the CCA-F exam.</b> This lesson is weighted to <b>Domain 1 — Agentic Architecture &amp; Orchestration (27%)</b>, the heaviest graded domain, with strong <b>Domain 2 — Claude Code Configuration (20%)</b> crossover. Expect questions on ...
L270 (x1): <b><span class="emoji">⚠️</span> Point-in-time note (preserved from the source).</b> This lesson states several <em>version-sensitive</em> facts — the SDK field rosters, the <code class="inline">Task</code>→<code class="inline">Agent</code> rename at <b>v2.1.6...
L281 (x1): <!-- LESSON 1 — Hook -->
L284 (x1): <h3 class="ltitle">Hook — subagents as a thing you <em>program</em></h3>
L295 (x1): <p><strong>Why before how:</strong> in the base course you met subagents through the <code class="inline">/agents</code> command — a tabbed UI where you click "create," fill in a description, pick tools, and save. That's the <em>operator's</em> view. The archi...
L299 (x1): <li><strong>Programmatically</strong> — the <code class="inline">agents</code> parameter in <code class="inline">query()</code> options. This is "the documented recommended approach for SDK apps."</li>
L300 (x1): <li><strong>Filesystem-based</strong> — Markdown files at <code class="inline">.claude/agents/*.md</code> with YAML frontmatter.</li>
L301 (x1): <li><strong>The built-in <code class="inline">general-purpose</code> subagent</strong> — always invocable via the Agent tool, with no definition at all.</li>
L304 (x1): <p><span class="analogy">Analogy</span> The CLI <code class="inline">/agents</code> UI is like hiring through a form on a careers page; the SDK <code class="inline">AgentDefinition</code> is like writing a job description in code that a factory function can ge...
L338 (x1): <div class="fbmsg" hidden>For SDK apps the recommended path is programmatic — pass an <code class="inline">agents</code> record/dict of <code class="inline">AgentDefinition</code>s into <code class="inline">query()</code>. Filesystem files suit version-control...
L342 (x1): <div class="cbody"><p>A subagent is not a UI artifact — it's a <strong>declarable object</strong>. Three doors lead to it (programmatic / filesystem / built-in <code class="inline">general-purpose</code>), but they all resolve to the same runtime worker. Archi...
L345 (x1): <!-- LESSON 2 — AgentDefinition -->
L348 (x1): <h3 class="ltitle">The <code class="inline">AgentDefinition</code> object — every field</h3>
L354 (x1): <li>Write the TS and Python shapes — and avoid the camelCase-in-Python <code class="inline">TypeError</code></li>
L359 (x1): <p>A subagent is declared as an <strong><code class="inline">AgentDefinition</code></strong>: a record (TS) / dataclass (Python) holding the subagent's <strong>routing description</strong>, <strong>system prompt</strong>, <strong>tool allow-list</strong>, <str...
L363 (x1): <div class="codelabel">TypeScript — <span class="verify">VERIFY</span> field roster against the live TS reference</div>
L383 (x1): <div class="codelabel">Python — note the field order differs; required fields first</div>
L401 (x3): <div class="cbody"><p><code class="inline">AgentDefinition</code> field names use <strong>camelCase even in Python</strong> — <code class="inline">disallowedTools</code>, <code class="inline">permissionMode</code>, <code class="inline">maxTurns</code>, <code c...
L403 (x1): <h4>SVG (a) — the <code class="inline">AgentDefinition</code> anatomy</h4>
L412 (x1): <text class="m" x="50" y="92">description</text><text class="s" x="50" y="106">routing signal — when to use it</text>
L458 (x2): <div class="verifynote"><b><span class="emoji">⚠️</span> <span class="verify">VERIFY</span> (preserved from source):</b> there is <b>no <code class="inline">mode</code> or <code class="inline">source</code> field</b> in the current <code class="inline">AgentDe...
L464 (x1): <div class="ans"><strong>All of them.</strong> "If omitted, <code class="inline">tools</code> inherits all tools." To restrict a subagent, you must explicitly list an allow-list (or use <code class="inline">disallowedTools</code> as a denylist). Omitting <code...
L475 (x1): <div class="fbmsg" hidden><code class="inline">AgentDefinition</code> fields are camelCase even in Python — <code class="inline">maxTurns</code>, not <code class="inline">max_turns</code>. A snake_case keyword raises a <code class="inline">TypeError</code> at ...
L479 (x1): <!-- LESSON 3 — the agents parameter -->
L482 (x1): <h3 class="ltitle">The <code class="inline">agents</code> parameter — programmatic vs. filesystem</h3>
L498 (x1): <p>In both, <strong>the key is the agent's name</strong> and the value is its definition. Here is the verbatim Python invocation — note <code class="inline">"Agent"</code> in <code class="inline">allowed_tools</code> (we'll see why in §4):</p>
L500 (x1): <div class="codelabel">Python — the <code class="inline">agents={...}</code> query example (verbatim)</div>
L530 (x1): <p>The TS equivalent uses <code class="inline">allowedTools: [..., "Agent"]</code> and <code class="inline">agents: { "code-reviewer": { description, prompt, tools, model } }</code> — the same shape, camelCase throughout.</p>
L532 (x1): <h4>Precedence — who wins on a name clash</h4>
L539 (x1): <rect x="40" y="18" width="620" height="40" rx="10" fill="#6b5b95"/><text class="w" x="350" y="43" text-anchor="middle">PROGRAMMATIC agents={} — wins over any filesystem agent of the same name</text>
L557 (x1): <tr><td>The definition is generated in code</td><td>Windows workaround: long programmatic prompts can hit the 8191-char command-line limit — use file-based instead</td></tr>
L588 (x1): <!-- LESSON 4 — runtime delegation -->
L591 (x1): <h3 class="ltitle">Runtime delegation — the Agent (formerly Task) tool</h3>
L604 (x1): <h4>SVG (c) — the delegation flow</h4>
L629 (x2): <div class="verifynote"><b><span class="emoji">⚠️</span> <span class="verify">VERIFY</span> — the Task → Agent rename (v2.1.63):</b> "The tool name was renamed from <code class="inline">"Task"</code> to <code class="inline">"Agent"</code> in Claude Code v2.1.6...
L636 (x1): <li><strong>Automatic</strong> — Claude matches the task to a subagent's <code class="inline">description</code> and delegates on its own.</li>
L637 (x2): <li><strong>Explicit</strong> — you name it in the prompt: <em>"Use the code-reviewer agent to check the authentication module"</em> — which bypasses automatic matching.</li>
L645 (x1): <div class="ans"><strong>Add <code class="inline">"Agent"</code> to <code class="inline">allowedTools</code>.</strong> Without it, the delegation tool isn't auto-approved — invocations fall to the <code class="inline">canUseTool</code> callback or get denied i...
L652 (x1): <button class="opt">Nowhere — it was fully removed</button>
L656 (x1): <div class="fbmsg" hidden>Current SDK releases emit <code class="inline">"Agent"</code> in <code class="inline">tool_use</code> blocks but still use <code class="inline">"Task"</code> in the <code class="inline">system:init</code> tools list and in <code class...
L660 (x1): <!-- LESSON 5 — single channel -->
L663 (x1): <h3 class="ltitle">The prompt string is the ONLY channel — "fresh but not empty"</h3>
L678 (x1): <h4>SVG (d) — what crosses the boundary vs. what doesn't</h4>
L700 (x1): <figcaption>"Fresh but not empty." The subagent gets its own prompt, the Agent tool's prompt, CLAUDE.md, and tool defs — never the parent's history, results, or system prompt; never unlisted skill content.</figcaption>
L714 (x1): <p><span class="analogy">Analogy</span> Delegating to a subagent is like emailing a contractor who has never seen your project: they know their own trade (system prompt) and the company handbook (CLAUDE.md), but they did <em>not</em> sit in your earlier meetin...
L716 (x1): <div class="callout tip collapsed"><div class="chead" onclick="toggleCall(this)"><span class="emoji">🧰</span> Design rule — self-contain every delegation<span class="chev">▼</span></div>
L717 (x1): <div class="cbody"><p>Pass concrete <strong>file paths</strong>, exact <strong>error strings</strong>, and prior <strong>decisions</strong> in the invocation prompt. If a rule must reach the subagent (e.g. "ignore <code class="inline">vendor/</code>"), <strong...
L733 (x1): <!-- LESSON 6 — context isolation -->
L736 (x1): <h3 class="ltitle">Context isolation — why subagents protect the main thread</h3>
L749 (x2): <p>The engineering framing: "Subagents use their own isolated context windows, and only send relevant information back to the orchestrator" — ideal for "tasks requiring filtering through large datasets where most information won't be useful." This ties to the ...
L757 (x1): <rect x="40" y="80" width="140" height="14" rx="4" fill="#e3f0e6"/><text class="s" x="110" y="112" text-anchor="middle">window stays small —</text><text class="s" x="110" y="128" text-anchor="middle">holds only the summary</text>
L772 (x1): <p><strong>When to reach for it.</strong> Use a subagent "when a side task would flood your main conversation with search results, logs, or file contents you won't reference again" — e.g. "run the test suite and report only the failing tests with their error m...
L782 (x1): <div class="fbmsg" hidden>Isolation means the search results/files accumulate in the subagent's own window, and only a concise summary returns — protecting the main thread's budget. (Subagents can't spawn subagents, and their transcripts are immune to main-con...
L789 (x1): <h2>The rules that keep delegation safe — and the graduation to Workflows</h2>
L793 (x1): <!-- LESSON 7 — guardrails -->
L796 (x1): <h3 class="ltitle">Guardrails — no nesting, least privilege, model, permissions, turns</h3>
L808 (x1): <p>"<strong>Subagents cannot spawn their own subagents. Don't include <code class="inline">Agent</code> in a subagent's <code class="inline">tools</code> array.</strong>" If your workflow needs nested delegation, "use Skills or chain subagents from the main co...
L810 (x1): <h4>SVG (e) — the no-nesting rule</h4>
L832 (x1): <h4>Least privilege — tool allow-lists</h4>
L835 (x1): <h4>SVG (f) — least-privilege tool combos</h4>
L844 (x1): <figcaption>Grant only what the job needs. "Full access" = omit <code class="inline">tools</code> — convenient, but the opposite of least privilege.</figcaption>
L847 (x1): <div class="verifynote"><b><span class="emoji">🔒</span> Never-available tools:</b> some UI/session tools are <b>never</b> available to subagents even if listed — <code class="inline">Agent</code>, <code class="inline">AskUserQuestion</code>, <code class="inlin...
L856 (x1): <p><code class="inline">maxTurns</code> caps agentic turns before the subagent stops. <span class="verify">VERIFY</span>: no explicit wall-clock <em>timeout</em> field is documented for <code class="inline">AgentDefinition</code> — verify timeout behavior agai...
L862 (x1): <div class="ans"><strong>Not allowed.</strong> "Subagents cannot spawn their own subagents" — never put <code class="inline">Agent</code> in a subagent's <code class="inline">tools</code>. The right alternatives: <strong>use Skills</strong> for nested behavior...
L877 (x1): <!-- LESSON 8 — CLI <-> SDK mapping -->
L880 (x1): <h3 class="ltitle">CLI ↔ SDK mapping — the 1:1 field bridge</h3>
L904 (x1): <p>Only <code class="inline">name</code> and <code class="inline">description</code> are required in frontmatter. The Markdown <strong>body</strong> is the system prompt — i.e. the SDK <code class="inline">prompt</code>.</p>
L906 (x1): <h4>SVG (b) — frontmatter ↔ <code class="inline">agents:{}</code></h4>
L928 (x2): <text class="s" x="402" y="232" style="fill:#b3402f">— NOT in the SDK type —</text>
L944 (x1): <tr><td><code class="inline">/agents</code> UI</td><td>(no SDK equivalent — code defines agents directly)</td></tr>
L953 (x1): <tr><td>frontmatter <code class="inline">hooks</code>/<code class="inline">isolation</code>/<code class="inline">color</code></td><td><b>filesystem-only</b> — not in the SDK type</td></tr>
L959 (x1): <div class="verifynote"><b><span class="emoji">⚠️</span> <span class="verify">VERIFY</span>:</b> <code class="inline">hooks</code>, <code class="inline">isolation</code>, and <code class="inline">color</code> appear in filesystem/CLI frontmatter but are <b>not...
L973 (x1): <!-- LESSON 9 — Workflow graduation -->
L976 (x1): <h3 class="ltitle">The Workflow-tool graduation — who holds the plan</h3>
L987 (x1): <p>"A dynamic workflow is a JavaScript script that orchestrates subagents at scale. Claude writes the script for the task you describe, and a runtime executes it in the background while your session stays responsive." The orchestration loop, branching, and int...
L991 (x1): <h4>SVG (g) — subagent delegation vs. Workflow at scale</h4>
L997 (x1): <text class="h" x="170" y="22" text-anchor="middle">SUBAGENTS — a few per turn</text>
L1007 (x1): <text class="h" x="528" y="22" text-anchor="middle">WORKFLOW — dozens to hundreds</text>
L1034 (x1): <p><strong>Patterns it unlocks.</strong> A workflow can have "independent agents adversarially review each other's findings before they're reported, or draft a plan from several angles and weigh them against each other." The bundled <code class="inline">/deep-...
L1036 (x2): <div class="verifynote"><b><span class="emoji">⚠️</span> <span class="verify">VERIFY</span> — caps &amp; version gates:</b> the Workflow runtime caps at <b>≤16 concurrent agents</b> (fewer on limited cores) and <b>1,000 agents total per run</b>. The <code clas...
L1043 (x1): <div class="q">You need to migrate 500 files with a coordinated, rerunnable orchestration that doesn't blow up the conversation's context. Subagents or a Workflow — and why?</div>
L1045 (x1): <div class="ans"><strong>A Workflow.</strong> Subagents suit "a few delegated tasks per turn." A 500-file migration coordinates far more agents than one conversation can hold, and you want the orchestration "codified as a script you can read and rerun." The sc...
L1060 (x1): <!-- LESSON 10 — design heuristics -->
L1063 (x1): <h3 class="ltitle">Design heuristics — subagent vs. a single longer prompt</h3>
L1068 (x1): <li>Decide when a subagent beats a single longer prompt — and when to stay in the main conversation</li>
L1085 (x1): <p>For a quick question about <em>existing</em> context, prefer <code class="inline">/btw</code> over a subagent — spinning up a fresh worker just to answer something already in the conversation is wasted latency.</p>
L1104 (x1): <div class="fbmsg" hidden>Stay in the main conversation when multiple phases share significant context — the single-channel isolation would force you to re-pass everything. The other three are textbook subagent fits (verbose output, summary return, enforced to...
L1112 (x1): <p>Ten source-grounded questions spanning the whole lesson — weighted toward CCA-F Domain 1 (the delegation model, isolation, no-nesting, single channel) with Domain 2 crossover (CLI ↔ SDK mapping, tool allow-lists, model selection). No hints; answer, then che...
L1126 (x1): <button class="opt">Work fine — snake_case is the Python convention</button><button class="opt">Silently ignore the field</button><button class="opt">Raise a <code class="inline">TypeError</code> at construction (fields are camelCase)</button><button class="op...
L1132 (x1): <button class="opt">Inherits all tools (the "full access" combo)</button><button class="opt">Gets no tools at all</button><button class="opt">Gets only <code class="inline">Read</code></button><button class="opt">Raises an error — <code class="inline">tools</c...
L1139 (x1): <div class="fb"></div><div class="fbmsg" hidden>"The only channel from parent to subagent is the Agent tool's prompt string." Include file paths, errors, and decisions directly in it — there's no shared memory.</div>
L1145 (x1): <div class="fb"></div><div class="fbmsg" hidden>The parent's history/results and system prompt never cross. The subagent does get its own prompt, the Agent tool's prompt, CLAUDE.md, and tool defs — "fresh but not empty."</div>
L1151 (x1): <div class="fb"></div><div class="fbmsg" hidden>Include <code class="inline">Agent</code> in the main <code class="inline">allowedTools</code>. (Putting <code class="inline">Agent</code> in a <em>subagent's</em> tools is forbidden — that would enable nesting.)...
L1161 (x1): <div class="qlab">Q8</div><div class="qtext">Programmatic vs. filesystem agents with the same name — which wins?</div>
L1186 (x1): <li>Claude Agent SDK — <em>Subagents in the SDK</em>: <a href="https://code.claude.com/docs/en/agent-sdk/subagents">code.claude.com/docs/en/agent-sdk/subagents</a></li>
L1187 (x1): <li>Claude Agent SDK — <em>TypeScript reference</em>: <a href="https://code.claude.com/docs/en/agent-sdk/typescript">code.claude.com/docs/en/agent-sdk/typescript</a></li>
L1188 (x1): <li>Claude Agent SDK — <em>Python reference</em>: <a href="https://code.claude.com/docs/en/agent-sdk/python">code.claude.com/docs/en/agent-sdk/python</a></li>
L1189 (x1): <li>Claude Code — <em>Create custom subagents</em>: <a href="https://code.claude.com/docs/en/sub-agents">code.claude.com/docs/en/sub-agents</a></li>
L1190 (x1): <li>Claude Code — <em>Workflows</em>: <a href="https://code.claude.com/docs/en/workflows">code.claude.com/docs/en/workflows</a></li>
L1191 (x1): <li>Anthropic — <em>Building agents with the Claude Agent SDK</em>: <a href="https://claude.com/blog/building-agents-with-the-claude-agent-sdk">claude.com/blog/building-agents-with-the-claude-agent-sdk</a></li>
L1192 (x1): <li>Anthropic — <em>Building effective agents</em>: <a href="https://www.anthropic.com/engineering/building-effective-agents">anthropic.com/engineering/building-effective-agents</a></li>
L1199 (x1): <p style="margin:4px 0"><strong>Extension S-E3 — Orchestrator-Worker at Scale + Failure Modes.</strong> You now have the primitive (this lesson) and the patterns (S-E1). S-E3 puts them under load: a production orchestrator-worker system, what breaks at scale, ...
L1200 (x1): <p style="margin:6px 0 0"><a href="orchestrator-worker-at-scale.html">Continue → Orchestrator-Worker at Scale + Failure Modes (S-E3)</a> <span style="color:var(--ink-soft);font-size:13px">— not built yet; this forward link may 404.</span></p>
L1204 (x1): <p><span class="sourcebadge"><span class="emoji">📄</span> Extension material · built faithfully from the captured citations file — every claim traces to an Anthropic source. No external facts introduced; <span class="verify">VERIFY</span> flags preserved; auth...
L1205 (x1): <p style="margin-top:14px">Subagents as a Programmable Primitive · Extension S-E2 for the Claude Certified Architect — Foundations prep · <a href="../introduction-to-subagents.html" style="color:var(--accent)">← Back to base course</a> · <a href="orchestration...
L1217 (x1): {title:"The primitive",lessons:[[1,"Hook — subagents you program"],[2,"The AgentDefinition object"],[3,"The agents parameter"]]},
L1219 (x1): {title:"Guardrails & scale",lessons:[[7,"Guardrails — no nesting, least privilege"],[8,"CLI ↔ SDK mapping"],[9,"The Workflow graduation"],[10,"Design heuristics"]]}
L1341 (x1): if(pct>=75){box.classList.add('pass');box.innerHTML='🎉 '+score+'/'+total+' — strong work! The subagent primitive is locked in.';}
L1342 (x1): else if(pct>=50){box.classList.add('mid');box.innerHTML='👍 '+score+'/'+total+' — solid. Skim the misses above and you\'re set.';}
L1343 (x1): else{box.classList.add('mid');box.innerHTML='📖 '+score+'/'+total+' — worth a re-read before moving on.';}